class InitialAdminConfig {
  const InitialAdminConfig._();

  // Change these values before distributing the first production build.
  static const username = 'admin';
  static const fullName = 'System Administrator';
  static const password = 'ChangeMe123!';
}